
\chapter{Internship Project}


\section{Customer Overview}
Christian Hintz Vermögensverwaltung GmbH is an independent German asset management company based in Stuttgart. The firm specializes in providing discretionary wealth management services for private investors and businesses, focusing on individually tailored investment strategies and portfolio management. Operating as a regulated financial institution under the supervision of the German Federal Financial Supervisory Authority (BaFin), the company offers services such as strategic portfolio management, financial planning, and investment advisory with a strong emphasis on long-term, goal-oriented wealth building.
\section{Customer KVG}
A Capital Management Company is a regulated financial institution that manages investment funds for investors. Its main tasks include portfolio management, risk management, and ensuring compliance with regulations. It also makes sure that investment strategies are followed according to the fund’s goals. In many cases, it acts as a link between asset managers, custodians, and other financial service providers, while being supervised by the relevant financial authority in its country.

In Germany, this type of institution is called a KVG (Kapitalverwaltungsgesellschaft) and is regulated under the German Capital Investment Code (KAGB) by BaFin. 

In this project, the client’s Capital Management Company is Monega Kapitalanlagegesellschaft mbH. Monega is a German investment management company that provides fund administration and asset management services. In this context, Monega acts as the data provider for client investment information, making sure that structured and validated financial data is available for processing and reporting.
\section{Preparation}
Proje süreci boyunca musteri ve yazilimci arasinda köprü görevi kuran project manager ile calistim. Project manager müsteri ile birlikte proje icin requirementlari belirledi ve Jira Taskte bunlari listeledi. Ben de bu sayede bir roadmap olusturabildim. 
Musteriden gelen Input verilerinin kaydedilebilmesi icin once sirketin internal sisteminde bulunan database scripts reposunda setup scripts dosyalarini olusturacaktim sonrasinda yine internal sistemdeki document enginede parser implemente edicektim ve en son yine burada otomatize facthseet olusumu icin parser implemente edicektim. 
Tüm bu plan icin version control sistemi Git ile repositorylere projeye uygun branchler olusturarak baslangic yaptim.  
\section{Requirements}
The scope of the project covers the generation of factsheets for the AI Leaders fund. The fund is structured into two share classes, AI Leaders A and AI Leaders C. 
Within this project, the AI Leaders C investment class has been implemented first, serving as a base template for the AI Leaders A class as well. 
This design allows both share classes to be generated using the same template with different setup scripts, ensuring reusability and consistency. 
The main characteristics of the project can be summarized as follows: 
\begin{itemize} 
    \item Factsheets can be generated and delivered at configurable time intervals based on future client requirements. 
    \item The existence of two share classes is due to differences in fees and expense structures, which may result in different performance outcomes for the same fund. 
    \item Input data is provided by the client’s KVG in XLSX format and is processed through the implemented application before being stored in a database. 
    \item Project requirements, including sample data files, are shared via Jira as attachments by the project manager for testing purposes. 
    \item The project manager defines the factsheet structure, including statistics, charts, and logos, in collaboration with the client and provides these requirements for implementation in the parser. 
    \item Chart styling elements such as color palettes, value ranges, and text annotations are predefined during the design phase. 
\end{itemize} 
Jaspersoft is a flexible reporting and analytics platform used to transform raw data into structured and readable reports. It retrieves both static and dynamic data from a configured database, enabling the creation of interactive dashboards, visual charts, and PDF reports. 
Overall, factsheets can be generated at any time using the stored database information in combination with the configured Jaspersoft template.


% Görev kapsami musteri icin Ai Leaders fonuna ait factsheetleri hazirlanmasini kapsamaktadir. Ai Leaders C ve Ai Leader A olarak iki pay sinifina ayrilan fonda Ai Leaders C investment classi implemente edilmistir.  Bu investmen class implementasyonu Ai LEaders A investment classi icin de bir base olusturmaktadir ve ayni template uzerinden farkli setup scriptleri ile olusturulmasina olanak saglar. 
% Bu investment classlara ait factsheetler musterinin gelecekteki taleplerine gore dilenilen zaman araliklarinda olusturulup musteriye iletilebilecektir. 

% Bu iki farklı pay sınıfının bulunmasının nedeni, her birinin farklı ücret ve giderlere tabi olmasıdır. Bu farklı ücretler nedeniyle aynı fonun farklı pay sınıfları farklı performans (getiri) gösterebilir.

% Factsheetlerin dayandigi bilgiler musterinin KVGsi tarafindan saglanan xlsx uzantili dosyalardan implemente ettigim program araciligiyla alinip database e kaydedilecektir
% Project manager bu dosyalarin ornegini Jira ortamina attachment olarak ekler, sonrasinda test edilebilmesi icin 

% Project manager musteri ile birlikte factsheette bulunacak istatistikleri, grafikleri ve logolari kararlastirarak bir jaspersoft templatei olsuturur ve bunlari parserda implemente edebilmem icin bana requirements olarak iletir.
% Chartlar icin style verileri ornegin renk paleti, sayi araliklari, text annotationlari belirlenir
% Jaspersoft is a flexible reporting and analytics platform used to turn raw data into clear, easy-to-read visuals, charts, and documents. Konfigure edilen databaseden dynamic ve static verileri ceker, allowing businesses to view interactive dashboards, generate PDFs, and create custom reports.

% Database'e kaydedilen bilgiler ve jaspersoft template ile dilenildigi zaman factsheetler olsuturabilmektedir. 



charts:
\begin{itemize}
    \item Performance Chart, rollierende volatibility chart, max drawdown chart 
    \item asset allocation, country allocation, sector allocation, currency allocation ring chart 
    \item Top 10 security positionen bar chart 
\end{itemize}

statistics:
\begin{itemize}    
    \item monthly and yearly wertentwicklung statistics 
    \item risikokenzahlen for year 1 
\end{itemize}

\noindent
The tables below represent the column structure of the XLSX files provided by the client. 
Data is extracted and processed based on these defined columns:



\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|c|c|c|c|}
\hline
A & E & F & G & H & S & V \\
\hline
AS\_OF\_DATE & NAME & WKN & ISIN & CURRENCY & FUNDVOLUME & PRICE \\
\hline
\end{tabular}
\caption{FondsKompakt Input Column Mapping}
\label{tab:fondskompakt-columns}
\end{table}


\begin{table}[H]
\centering
\begin{tabular}{|c|c|}
\hline
AA & AB \\
\hline
DIVIDEND & DIVIDEND\_DATE \\
\hline
\end{tabular}
\caption{FondsKompakt Input Column Mapping}
\label{tab:fondskompakt-columns}
\end{table}


\begin{table}[H]
\centering
\begin{tabular}{|c|c|c|c|c|c|c|}
\hline
E & F & G & I & P & W & V \\
\hline
ISIN & SECURITY NAME & ASSET & CURRENCY & MARKET VALUE & ISSUER & SECTOR  \\
\hline
\end{tabular}
\caption{Bestand Input Column Mapping}
\label{tab:bestand-columns}
\end{table}

\begin{table}[H]
\centering
\begin{tabular}{|c|}
\hline
AC \\
\hline
COUNTRY \\
\hline
\end{tabular}
\caption{Bestand Input Column Mapping}
\label{tab:bestand-columns}
\end{table}



\section{Implementation}
\subsection{Setup Scripts}
Musterilerden gelen veriler databankalarina direkt manuel olarak eklenmemektedir. Bunun icin bir database-scripts adinda bir repo bulunur ve setup scripleri sayesinde data manipulation uygulanabilmektedir.
Bu scriptlerde standardised opsiyonel ve zorunlu datacolumnlar vardir ve bunlar her bir musteri ve proje icin ozel olarak doldurulur. Bu sekilde sistem genelinde bir structure korunur.

Bu dosyalar bash scriptler araciligiyla belirli input klaosrlerine yonelndirilir ve burada otomatik olarak input parsera atanir. Parser CSV doyasini okur ve veritabani kayitlarini olusturur.

Musteriyi kayit etmek ve projesinin temelini olusturmak amaciyla 12 tane script dosyasi hazirlanip proje icin gerekli temel bilgiler database e kaydedilmistir. 
Bu scriptler arasinda bagimlilik bulundugundan yurutme sirasina gore runlanir. Iste o scriptler:

\begin{itemize}
    \item \texttt{01\_customer\_setup.csv:} customer setup ile database e yeni bir customer eklenir. Bu estup basitce customerin adi, internal sistemde kolaylik olmasi acisindan bir nickname ve yiine bir java classi ile uretilen bir hash key ile kaydedilir. 
    \item \texttt{02\_project\_setup.csv:} Project setup ile var olan musterilere yeni projeler eklenir. Bu projelerin componentleri, template bilgileri, error mail recipienti, uretim icin gerekli file amounti ve ultimo regionu csvde eklenir. Örnegin Hintzvv icin project setup dosyasi su sekildedir:
\begin{table}[h]
\centering
\footnotesize
\csvautotabular[
    separator=semicolon,
    respect all
]{project_setup.csv}
\caption{CSV Tablosu}
\end{table}

    \item \texttt{03\_fund\_setup.csv:} fund setup dosyasi ile musterinin yeni fundlari database e kaydedilir.
    \item \texttt{04\_fic\_setup.csv:} Fund investment class setup ile fondlara ait olan investment classlara veri bankasina kaydedilir.
    \item \texttt{05\_price\_series\_backfill.csv:} Die Series-Backfill-Setups werden genutzt, um zeitlich zurückliegende Daten für Zeitreihen zu einer bestehenden Reihe nachzutragen. Series backfill setups are used to add price series data back in time to an existing series. This is needed to evaluate and display statistics over a longer period of time. 
    \item \texttt{06\_insert\_input\_pattern.csv:} Input pattern setup i ile musteriden gelecek olan datalarin runlandiginda otomatik olarak ait oldugu input parser tarafindan okunabilmesi icin bir file input pattern olusturmayi saglar.
    \item \texttt{07\_staticdatasetup.csv:} Factsheetta bulunan static datalarin templatede attribute araciligyla kullanilabilmesi icin veribankasina kaydedilmesini saglar. Bu datalar nadir durumlarda degisiklik gosterir ornegin country veya issue fee gibi
    \item \texttt{08\_classification\_backfill.csv:} Classification backfll ile fon icin gerekli temel classification categori bilgisini kaydeder. Bu classificationlar daha sonrasinda cesitli chartlari olustururken kullanilir. Bu proje icin gerekli classificationlar sunlardir:\\
    - AI Leaders asset classification \\
    - AI Leaders sector classification \\
    - AI Leaders country classification \\
    - AI Leaders currency classification
    \item \texttt{09\_benchmark\_series\_setup.csv:} To Map different existing time series as benchmarks to either funds, investment classes and depots. 
    \item \texttt{10\_setup\_email\_list.csv:} Email setup ile factsheet icin bir email listesi olusturuldu. E-Mail- Adressen, welche der Liste hinzugefügt werden, erhalten automatisiert die in Zukunft erzeugten Factsheets.
    \item \texttt{11\_map\_email\_list.csv:} Hier wird die E-Mail-List ID auf die ID des Projektes gemappt.
    \item \texttt{sql\_function.csv:} First of all: This is not a "real" SQL function. This .csv file contains the field names and the corresponding data types. The .csv is processed by the SqlFunctionParser. The parser creates a SQL function from this file that can display the associated data on the corresponding fields on the template.
\end{itemize}


\subsection{Document Engine}
Document engine internal sistemde projelerin olsuturldugu repodur ve en merkez reponun bu oldugu soylenebilir. Burada her bir proje icin bir parser bulunur. 
Projeden kasit, X musterisi icin datalarin data bankasina kaydedilmesi de bir projedir ve bunun icin de gerekli miktarda parser gelistirilir, ayni sekilde factsheetler icin de gecerlidir.
Bunun yani sira databse scriptlerinin de calismasini saglayan parserlar da burda bulunur. 
Burda ayni zamanda bircok service icin gerekli yardimci parent classslar ve interfaceler bulunur. Her proje ve attribute icin belirli standardda kalmak adina bu gerekli componentler de object oritented olarak bulunmaktadir.
Document enginedeki gorevim temel olarak 4 basliga ayriliyor:

\subsubsection{Input Parser}
Input Parser usstte de orneklendigi gibi musteriden gelen datalarin veribankasina consistent ve maintanable kaydedilmesini saglar. Burada her bir parser spring component olarak implemente edilir 
ve prototype olarak instancelari olsuturlur. Burada en onemli method parse\(\)'dir. 

\begin{lstlisting}[caption={method}]
public void parse(DocumentOld document) {
	priceUpdates = input.getHeaderlessSheetDataFromExcel(document.getSingleDataFile(), "Fonds_Kompakt")
		                .stream()
		                .filter(ISIN_COLUMN::isNotBlank)
		                .map(this::toPriceUpdate)
		                .flatMap(Optional::stream)
		                .toList();

	Map<InvestmentClassPriceSeriesUpdateInfo, InvestmentClassPriceSeriesUpdate> updates =
			priceUpdates.stream()
				        .collect(toMap(
						        HintzPriceUpdateInfo::priceSeriesUpdateInfo,
						        HintzPriceUpdateInfo::priceSeriesUpdate
				        ));

	updateMongoService.savePriceUpdates(updates);
}
\end{lstlisting}


\begin{lstlisting}[caption={Incoming Security}]
@Data
public class MonegaHintzvvIncomingSecurity implements Security {

	private final String isin;
	private final String name;
	private final BigDecimal weighting;
	private final String assetType;
	private final String country;
	private final String equitySector;
	private final String currency;
    ...
    }

\end{lstlisting}

\begin{lstlisting}[caption={Incoming Security}]
private MonegaHintzvvIncomingSecurity toSecurity(String[] row, BigDecimal fundVolume) {
	String isin = ISIN_COLUMN.getOrThrow(row);
	String name = EMITTENT_NAME_COLUMN.getNonBlank(row).orElseGet(() -> SECURITY_NAME_COLUMN.getOrThrow(row));
	String asset = ASSET_COLUMN.getNullable(row);
	BigDecimal marketValue = MARKET_VALUE_COLUMN.getAsBigDecimal(row).divide(fundVolume, DECIMAL128);
	String currency = CURRENCY_COLUMN.getNullable(row);
	String country = COUNTRY_COLUMN.getNullable(row);
	String sector = SECTOR_COLUMN.getNullable(row);

	return new MonegaHintzvvIncomingSecurity(isin, name, marketValue, asset, country, sector, currency);
}

\end{lstlisting}

\begin{lstlisting}[caption={method}]
public void parse(DocumentOld document) {
List<String[]> data = input.getHeaderlessSheetDataFromExcel(document.getSingleDataFile(),
			"scd_bestand_kompakt");

	String kvgId = getFundKvgId(data);
	asOfDate = getAsOfDate(data);
	BigDecimal fundVolume = getFundVolume(data);

	List<MonegaHintzvvIncomingSecurity> incomingSecurities = 
        data.stream()
            .map(row -> toSecurity(row, fundVolume))
            .toList();

	FundUpdateInfo fundUpdateInfo = updateMongoService.findFundUpdateInfoForKvgId(kvgId, asOfDate);

	FundUpdate fundUpdate = updateFund(incomingSecurities, fundUpdateInfo, asOfDate, fundVolume);
	isinsToTrigger.addAll(fundUpdateInfo.getIsins());

	updateMongoService.save(fundUpdateInfo, fundUpdate);
}
\end{lstlisting}

\subsubsection{Chart Style Data}

\subsubsection{Charting}

\subsubsection{Factsheet Parser}

\section{Testing}